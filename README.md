# every-cent
A self-hosted personal finance analytics pipeline that automatically
ingests bank transaction data via the SimpleFIN API and transforms it
into daily cash flow, spending, and net worth insights — built with
Python (uv), DuckDB, and dbt, and designed to run entirely on free-tier
infrastructure without ever storing financial data unencrypted at rest.
 